spring:
  application:
    name: name-service
  devtools:
    livereload:
      port: 35729
server:
  port: ${port:8761}
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka

